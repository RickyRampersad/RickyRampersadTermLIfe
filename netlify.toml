# Netlify configuration for the Ricky Rampersad Branch site.

# Static site: the repository root is published as-is. The only server-side
# code is in netlify/functions (see PENSION-SETUP.md).
[build]
  publish = "."
  functions = "netlify/functions"

# Short personal links: /r/<token> → renewal portal with the token attached.
# These are the links stored in the Google Sheet's "Portal Link" column.
[[redirects]]
  from = "/r/*"
  to = "/renewal/?t=:splat"
  status = 302

# Convenience: /renew also opens the portal.
[[redirects]]
  from = "/renew"
  to = "/renewal/"
  status = 302

# Convenience: /pension opens the pension application wizard.
[[redirects]]
  from = "/pension"
  to = "/pension/"
  status = 301

# The short links for the launch page and the film are defined per domain
# below — a path-only rule here would match pensionplantt.com first and send it
# back through the catch-all as /pension/pension/launch.html.

# ── pensionplantt.com ────────────────────────────────────────────────────────
# The pension wizard has its own domain. Add pensionplantt.com and
# www.pensionplantt.com to this Netlify site (Domain management → Add domain),
# and these rules serve the wizard at the root of that domain while the rest of
# the site stays on its own domain. See PENSION-SETUP.md for the DNS records.
#
# RIA's endpoint has to be matched FIRST — the catch-all below would otherwise
# rewrite /api/pension-ai into /pension/api/pension-ai and the assistant would
# 404 on this domain only.
[[redirects]]
  from = "https://pensionplantt.com/api/pension-ai"
  to = "/.netlify/functions/pension-ai"
  status = 200
  force = true

[[redirects]]
  from = "https://www.pensionplantt.com/api/pension-ai"
  to = "/.netlify/functions/pension-ai"
  status = 200
  force = true

# pensionplantt.com/launch and /film — the two links the branch hands out.
# These are rewrites, so the short address stays in the bar, and they must be
# matched before the catch-all below. /start is kept as an alias so any link
# already given out keeps working.
[[redirects]]
  from = "https://pensionplantt.com/launch"
  to = "/pension/launch.html"
  status = 200
  force = true

[[redirects]]
  from = "https://www.pensionplantt.com/launch"
  to = "/pension/launch.html"
  status = 200
  force = true

[[redirects]]
  from = "https://pensionplantt.com/start"
  to = "/pension/launch.html"
  status = 200
  force = true

[[redirects]]
  from = "https://www.pensionplantt.com/start"
  to = "/pension/launch.html"
  status = 200
  force = true

[[redirects]]
  from = "https://pensionplantt.com/film"
  to = "/pension/how-it-works.html"
  status = 200
  force = true

[[redirects]]
  from = "https://www.pensionplantt.com/film"
  to = "/pension/how-it-works.html"
  status = 200
  force = true

# Everything else on the domain is the wizard. status 200 rewrites rather than
# redirects, so the address bar keeps showing pensionplantt.com.
[[redirects]]
  from = "https://pensionplantt.com/*"
  to = "/pension/:splat"
  status = 200
  force = true

[[redirects]]
  from = "https://www.pensionplantt.com/*"
  to = "/pension/:splat"
  status = 200
  force = true

# The same short links on the branch's own domain, where nothing rewrites the
# root and a plain redirect is right.
[[redirects]]
  from = "/launch"
  to = "/pension/launch.html"
  status = 301

[[redirects]]
  from = "/start"
  to = "/pension/launch.html"
  status = 301

[[redirects]]
  from = "/film"
  to = "/pension/how-it-works.html"
  status = 301

[[headers]]
  for = "/renewal/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"

# The wizard holds client data in the browser only — keep it out of frames and
# out of referrer headers.
[[headers]]
  for = "/pension/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"

# The blank Guardian forms and the PDF engine are ~2.9 MB and never change
# without a filename change, so let the browser keep them for a year.
[[headers]]
  for = "/pension/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
